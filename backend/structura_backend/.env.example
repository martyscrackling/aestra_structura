# Django
DEBUG=1
SECRET_KEY=ff1ecc04f8a93b1bc5a4503040aded76
ALLOWED_HOSTS=localhost,127.0.0.1,structura-backend-4vxo.onrender.com

# Supabase Postgres (use the Supabase connection string; include sslmode=require)
DATABASE_URL=postgresql://postgres.prokngxytawscbszbnxc:z7UFVqD4aZ1B96H3@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres

# Web deployment (Flutter web URLs) - set in production
# If you're running Flutter Web locally (flutter run -d chrome), the origin is
# usually http://localhost:<random_port>. Use regex or the localhost flag below.
CORS_ALLOWED_ORIGINS=https://martyscrackling.github.io,https://structura-frontend-jc7s.onrender.com

# Optional: comma-separated regexes for allowed origins
# Example: ^http://localhost(:\d+)?$,^http://127\.0\.0\.1(:\d+)?$
CORS_ALLOWED_ORIGIN_REGEXES=

# Set to 1 to allow localhost/127.0.0.1 origins (handy for local Flutter Web).
CORS_ALLOW_LOCALHOST=1

# Set to 1 only if you explicitly want to allow all origins (not recommended).
CORS_ALLOW_ALL_ORIGINS=0

CSRF_TRUSTED_ORIGINS=https://martyscrackling.github.io,https://structura-frontend-jc7s.onrender.com

# Optional (usually keep enabled behind Render/Railway HTTPS)
SECURE_SSL_REDIRECT=1

# App branding / links (used by invitation + assignment emails)
APP_NAME=Structura Construction Corp
FRONTEND_URL=https://structura-frontend-jc7s.onrender.com

# Email delivery (optional)
# If EMAIL_HOST is empty, Django prints emails to the console.
DEFAULT_FROM_EMAIL=aestra462@gmail.com
# SMTP option
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=aestra462@gmail.com
EMAIL_HOST_PASSWORD=efzidfhnfrxxlawo
EMAIL_USE_TLS=1

# SendGrid option (recommended on hosts that block SMTP)
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=no-reply@aestra_structura.com
